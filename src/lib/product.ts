/**
 * The product name in one place, so renaming is a one-line change.
 *
 * "MUN Operations Software" is a working title taken from the repository name.
 * Shorter candidates worth considering later: Placard (the nameplate every
 * delegate holds), Dais (where the chairs sit and run the room), Quorum
 * (enough members to do business, which is what this manages).
 */
export const PRODUCT_NAME = 'MUN Operations Software'

/** Used in page titles as `<page> · <PRODUCT_SHORT_NAME>`. */
export const PRODUCT_SHORT_NAME = PRODUCT_NAME

export const PRODUCT_DESCRIPTION =
  'Run the operations of a Model UN conference: committees, delegates, allocations, logistics and awards.'
